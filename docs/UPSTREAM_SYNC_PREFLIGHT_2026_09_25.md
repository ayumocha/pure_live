# 上游同步预检与候选范围（2026-09-25）

状态：本文保留初始预检记录。用户随后已明确批准“独立分支完整同步，直接用上游斗鱼恢复机制”；后续逐提交、逐文件审查及最终处置以 `UPSTREAM_AUDIT_9b376ec9ef25.md` 为准。本文不是合并门禁通过或最新验证声明。

## 冻结基线与回退资料

| 项目 | 值 |
| --- | --- |
| 维护分支提交 | `e33795ccf8cd51ac5548ff960fa389282d8d6171` |
| 上游仓库 | `https://github.com/liuchuancong/pure_live` |
| 冻结上游提交 | `9b376ec9ef25b532296871073a39075f6f9db397` |
| 实际 merge base | `a218922bf837b22a6075db54c296912fa60b2584` |
| 上游源码版本 / Flutter | `3.1.5+4104` / `3.47.5` |
| 本分支版本 / Flutter | `3.0.7+4095` / `3.47.0` |

上游范围为 merge base 到冻结 SHA：1,255 个提交、1,950 个文件，278,892 行增加、37,056 行删除。机器规则归类为高风险的文件有 512 个；31 个应用/原生源码路径被删除，49 个文件为二进制变化。这是变更规模统计，不是缺陷数量。

本分支从 merge base 起有 34 个已提交的差异文件；只针对已提交 HEAD 的 `git merge-tree --write-tree --name-only --no-messages` 模拟发现 28 个冲突文件。模拟不修改分支、索引和工作树，也不包含尚未提交的斗鱼恢复补丁。完整清单在 `local-artifacts/upstream-reviews/pre-sync-20260925/merge-tree-head-only.txt`。

此前斗鱼补丁的 11 个代码/测试文件全部与测试包清单中的 SHA-256 相符，可复用原 91 项定向测试证据；这不代表它们已适用于新上游。已保存跟踪文件 patch、完整的相关代码/测试/说明和测试清单至 `local-artifacts/upstream-reviews/pre-sync-20260925/`。当前可运行程序、工作树代码和 master 指向未被替换。

## 已确认需要适配的项目

### 播放与斗鱼恢复

上游已经存在单一 `PlaybackSourceResolver` 和源提交机制：`lib/player/core/player_manager.dart` 在直播完成时发起取源，`lib/core/site/douyu/douyu_site.dart` 重新请求画质、CDN 和 URL，成功后更新界面。应将现有斗鱼补丁适配进该机制，避免叠加第二套 resolver、预算和恢复状态机。

初始预检曾建议移植本地终态归并。用户后续选择直接采用上游，因此不再叠加旧补丁；终态回写与慢取源列为需验证风险。保留上游新增的 source-generation fence、原生帧观测和旧会话事件丢弃，不根据已有恢复代码宣称所有真实播放场景已修复。

### 小红书身份与已有数据

本分支站点 ID 为 `xhs`，冻结上游为 `xiaohongshu`。上游 `FavoriteRoomController` 的目录规范化、偏好修正和收藏规范化未覆盖旧 `xhs`：目录可能被过滤，偏好可能重置；收藏和历史保留旧字符串却不能通过 `Sites.of` 找到站点。直接替换存在确定的兼容缺口。

建议在兼容层统一旧 ID，启动加载与备份恢复均覆盖目录、偏好、收藏、历史和去重；迁移应幂等，保留标签与观看元数据。上游新增官方 deep link 和 `/hina/livestream/`，但不接受本分支已支持的主播主页，动态路径规则也更窄；合并时需保留旧输入契约并补测试。纯视频布局继续按通用播放器不变量验收。

### 依赖、原生库与发布

- Flutter 从 3.47.0 升至 3.47.5；FFmpeg 包从精确 0.5.13 升至 0.6.2，并接入 FFmpeg 9.0.2 二进制；media-kit 改用新的统一 Git/native-assets 依赖布局。这属于工具链与播放器原生依赖迁移，不能只更新锁文件。
- `flv_lzc` Git 依赖未明确钉提交，锁文件的 ref 为 HEAD。需要保留维护分支本地修补，或核对实现后钉完整提交，并重新验证录制及打包契约。
- 上游修改了版本、更新下载地址、发布工作流和默认标签，部分源码版本与工作流默认标签并不一致。保留 `ayumocha/pure_live` 的身份、实际版本线、更新源和手动串行构建策略；不能复制上游发布元数据。
- 上游新增定时更新 Release 索引并推送 master 的工作流，不能直接启用。签名指纹在已检查路径未更换，新增签名校验可以独立评估。

## 门禁现状

1. 改动前已执行 `tool/validate_build_policy.ps1`，仍因既有工作流标记 `needs.android.result == 'success'` 不匹配退出，未作通过声明。
2. 已运行 `tool/review_upstream_update.ps1 -BaseRef HEAD -UpstreamRef 9b376ec9ef25b532296871073a39075f6f9db397 -ReportOnly`。机器证据为 `local-artifacts/upstream-reviews/upstream-9b376ec9ef25.json`，命令退出 1。
3. 空白诊断大量来自 CRLF；使用 `core.whitespace=cr-at-eol` 复核后剩 15 条诊断，不能把原始输出条数当成代码缺陷数量。
4. 凭据形态警报只定位到 `test/fixtures/tls/localhost-key.pem`；相邻 README 与测试调用表明它是公开、本地自签 TLS 测试夹具，不是已经证实的生产密钥泄漏。没有输出其内容。仍须在正式审计中依照凭据/夹具策略明确处置，不能直接忽略警报。
5. 可变引用警报包含真实的 `flv_lzc` HEAD 依赖，以及被泛化扫描误分类的 checkout `ref: master`。需分别处理真实依赖漂移与工作流行为，不通过虚假标记或删校验绕过门禁。

## 建议同步范围与完成条件

推荐在独立 `codex/` 分支完整迁移到冻结上游，保留当前 master 与可用测试包作为回退点。开始前先将已验证的本地斗鱼修复单独保存为提交，再完成全部入站提交/文件的语义台账。适配边界如下：

| 模块 | 计划处置 | 最小验收 |
| --- | --- | --- |
| 上游新增站点与普通功能 | 完整审查后逐项 accept/adapt，不默认信任 | 对应解析、契约和设置测试 |
| 播放器 / 斗鱼补丁 | 单一上游源模型，移植终态与取消保证 | EOF 时序、慢请求、暂停/关闭/换房、相同 URL、手动恢复 |
| 小红书 / 设置 / 备份 | 旧 ID 与链接兼容，保留现有用户数据 | 启动与备份迁移、幂等、重复项、旧分享入口 |
| Flutter / FFmpeg / media-kit | 审查并迁移原生依赖和本地修补 | 原生库内容/哈希、录制契约、Android/Windows 串行验证 |
| 版本 / 下载源 / 发布 | 保留本 fork 身份、版本规则和手动触发 | 版本一致性、工作流权限/输入/产物契约 |
| 维护策略与代理规则 | 遵循用户当前规则，审查上游差异 | 不自动运行设备脚本或更改认证/安全设置 |

正式合并前提交完整审计并通过强制门禁；合并后全仓审计、静态分析、受影响测试，正式交付再执行完整门禁。既有斗鱼证据不能替代升级后验证，上游自述的设备测试也不是本机验证。

备选范围是只回移 Windows / 斗鱼播放稳定性改动，保留现有架构和站点集合；这应明确称为选择性回移，而非已完成上游同步。

后续授权已确定完整同步，旧本地修复已保存在 `codex/backup-douyu-recovery-20260925`。工作继续在独立同步分支，主分支与用户当前程序保留；正式状态见后续审计文档。
